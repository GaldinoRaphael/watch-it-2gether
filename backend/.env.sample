POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=watch_it

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/watch_it?schema=public"
JWT_SECRET="sua_chave_secret_key"

TOKEN_TMDB=""
TMDB_API_KEY=""

# Production-only settings (leave blank in development)
# CORS_ORIGIN=https://seudominio.com
# NODE_ENV=production